#Instalar Docker .... suerte xd

#En raiz ejecutar lo siguiente
#docker compose build
#docker compose up

#Si eso no funciona, probar borrar compilaciones locales del servidor, borrar imagenes de docker de servidor anteriores y ejecutar de nuevo
