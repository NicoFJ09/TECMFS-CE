version: "3.8"

services:
  tecmfs-ce:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/app/uploads
    working_dir: /app
    command: ./servidor
